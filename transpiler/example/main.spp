adder = |int a, int b| => {
    die a + b;
}->int

boolCheck = |int a, int b, int exp| => {
    die (a + b) == exp;
}->bool

main = || => {
    say("Testing...\n");

    if (adder(2, 2) == 4): say("Adder works!\n"); else: say("Adder doesn't work\n");!

    if (1 != 1): say("1 isn't 1?!\n"); else: if (3 == 3): say("Three is three.\n");!!

    let test234[bool] = boolCheck(2, 2, 4);

    let marf[string] = "test123";

    die 0;
}->int
